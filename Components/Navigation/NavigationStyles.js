import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  navigationContainer : {
    position: 'absolute',
    bottom: 0,
    width: '100%',
    padding: 20,
    backgroundColor:'#7acc8f',
    flex:1,
    flexDirection:'row'
  }
})
